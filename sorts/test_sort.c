#include <stdio.h>
#include <stdlib.h>
#include <strings.h>
#include "data.h"

//Finds the difference from highest to lowest number in the list creates a list of empty arrays then numbers are put in each correct one
void array_sort(int *numbers, int *sort_number)
{
	int **number_lists;
	int highest_num, lowest_num, diff;




}
